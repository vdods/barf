// 2014.04.07 - Victor Dods

#pragma once

#include <lvd/literal.hpp>

namespace cbz {

using lvd::literal_of;

using lvd::EscapeStringReturnCode;
using lvd::EscapeStringStatus;

using lvd::char_literal_char_needs_normal_escaping;
using lvd::char_literal_of;
using lvd::char_needs_hex_escaping;
using lvd::escape_char;
using lvd::escape_code;
using lvd::escape_string;
using lvd::escaped_char;
using lvd::hex_char;
using lvd::hex_char_literal;
using lvd::string_literal_of;

} // end namespace cbz
